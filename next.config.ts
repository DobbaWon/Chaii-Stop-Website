/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",       // enables static HTML export
  distDir: "out",         // optional: folder for built files
};

module.exports = nextConfig;
